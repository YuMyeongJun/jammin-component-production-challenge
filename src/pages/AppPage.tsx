import { Input, Button } from "@components";

export const AppPage = () => {
  return (
    <>
      <Input />
      <Button variant="contained" color="info">
        test
      </Button>
    </>
  );
};
