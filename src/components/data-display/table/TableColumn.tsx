import { AnyObject } from '@models';

import { ITableColumnProps } from './Table.types';

export const TableColumn = <RecordType extends AnyObject = AnyObject>(
  args: ITableColumnProps<RecordType>,
) => {
  return null;
};
