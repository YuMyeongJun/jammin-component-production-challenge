import { AnyObject } from '@models';

import { ITableColumnGroupProps } from './Table.types';

export const TableColumnGroup = <RecordType extends AnyObject = AnyObject>(
  args: ITableColumnGroupProps<RecordType>,
) => {
  return null;
};
